require 'spec_helper'

describe ExampleThreadPool do
  it 'has a version number' do
    expect(ExampleThreadPool::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
